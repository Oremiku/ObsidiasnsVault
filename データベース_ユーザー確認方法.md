#web_practice
#DB 

```sql
-- ログインユーザー
SELECT name, type_desc
FROM sys.server_principals
WHERE type IN ('S', 'U', 'G');
```

```sql
-- データベースユーザー
use tempdb;

SELECT name, type_desc
FROM sys.database_principals
WHERE type IN ('S', 'U')
  AND sid IS NOT NULL;
```

