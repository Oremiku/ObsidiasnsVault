#git 

1.  ユーザー情報の定義
GitHubに登録したユーザー名およびメールアドレスと同じにする。
```markdown
git config -global user.name "[ユーザー名]"
```

```markdown
git config -global user.email [メールアドレス]
```


2.  GitHub上でリポジトリを作成
[初期設定方法.xlsx](file:D:\Git_Pracitice\GitHub初期設定.xlsx)
